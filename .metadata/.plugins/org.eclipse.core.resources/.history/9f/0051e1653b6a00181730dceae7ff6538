package com.ashutosh.shoppingcart.dao.impl;

import java.sql.Date;

import org.hibernate.SessionFactory;
import org.springframework.beans.factory.annotation.Autowired;

import com.ashutosh.shoppingcart.domain.Customerquery;
import com.ashutosh.shoppingcart.domain.User;

public class CustomerqueryDAOImpl {
	
	@Autowired
	private SessionFactory sessionFactory;

	@Autowired
	private Customerquery customerquery;
	
	public boolean save(Customerquery customerquery) {
		// get session from Session Factory
		// from SessionFactory
		// 1) Open new Session
		// 2) continue with Current Session
		try {
			// set current date
			//user.setAdded_date(new Date(System.currentTimeMillis()));
			sessionFactory.getCurrentSession().saveOrUpdate(customerquery);
		} catch (Exception e) {
			// print the complete exception stack trace
			e.printStackTrace();
			return false;
		}
		// above line : opening session saving user in user table.

		// TODO Auto-generated method stub
		return true;
	}
}
